#' An example to solve a system
#' @param rows `numeric` number of rows
#' @param cols `numeric` number of columns
#' @param iterations `numeric` number tries
#' @export
solveMatrix <- function(rows = 300, cols = 200, iterations = 20) {
    solve_problem(rows, cols, iterations)
}
